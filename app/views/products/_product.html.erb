<div class="col-xs-12 col-sm-6 col-md-4 col-lg-3">
  <div class="thumbnail">
    <%= link_to image_tag(product.image_url, class: "img-responsive img-product"), product %>
    <div class="caption">
      <h1><%= link_to(product.name, product) %></h1>
      <p><%= product.description %></p>
      <p><strong>
        <%= number_to_currency(product.price, unit: '€', precision: 2) %>
      </strong></p>
    </div>
  </div>
</div><!-- /col -->
